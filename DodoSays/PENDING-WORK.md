# PENDING-WORK — DodoSays

> 只装**欠着的活**(交付物是代码 / 功能 / 数据变更)。
> ⛔ 不装「要确认的事」——「真机验一下」「跑个 A/B 看看」没有完成态,永远不会有人来划掉它。
> 完成即删。

**当前:空。**

(2026-08-16 清掉「harness 反过来:默认全 secret」—— 已落地,
`tools/harness.lua` 递给插件的值默认 secret、裸字面量报错、明文要 `H.plain(…)`;
A/B 见 `CLAUDE.md` §2 那份变异清单。)
