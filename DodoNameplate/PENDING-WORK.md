# Pending Work — DodoNameplate

> **SOP**: TODO list. **Delete when done** -- copy the finished item into [`SESSION-LOG.md`](SESSION-LOG.md) Part B for that day, then remove it here. No strike-through DONE noise.

> **Status 2026-06-24 (v0.8.0 -- shipped)**: Phase 1 PvE done + dungeon-validated. **v0.8.0** = Phase 2 PvP base + the AURA feature + distribution polish. **Aura feature**: secret-safe CATEGORY aura icons on enemy plates (groups 5+6) mirroring Blizzard's nameplate categories -- 个人减益 `HARMFUL|PLAYER` + 重要减益 `HARMFUL|RAID(_IN_COMBAT)` in a LEFT row, 共享控制 `HARMFUL|CROWD_CONTROL` in a RIGHT CC slot, 敌方增益 `HELPFUL` (off by default); Blizzard C-side countdown number (`SetHideCountdownNumbers(false)`, renders under secret); shared `ns.db.auras` config + a dedicated "Auras"/光环 sub-page. **Per-spellID aura curation is DEAD on Midnight** (GOTCHAS S3). **Forbidden friendly plates fixed**: friendly party/raid plates in PvE instances are forbidden frames -> skipped cleanly, clean Blizzard default (GOTCHAS S2). **Name moved INSIDE the bar** for enemy plates (auto-sized). **Distribution**: addon icon (`Media/Dodo.tga`), Jerry's live config baked as defaults, language default = English. **PvP base**: enemy player (group 6) class-colored bar + cast bar, default ON, war mode / BG / arena; cast path hardened for secret cast names. **v0.7.1**: raid marker centered ABOVE the bar (global size `markSize`), elite icon INSIDE the bar (left, auto-sized), gray diagonal-stripe overlay on uninterruptible casts (`Media/stripe.tga`, secret-safe), cast spell-icon sized to cast-bar height, and two white current-target arrows flanking the bar (`Media/arrow.tga`, size = bar height, no config). Raid marker fixed for instances (`GetRaidTargetIndex` is secret -> feed it to the `SetRaidTargetIconTexture` SINK + pre-set the base texture). **In v0.7.0**: (a) hostile health bar RECOLORS (default white, configurable) while the unit casts an important spell -- secret-safe via `EvaluateColorValueFromBoolean(castImportant, special, threat)` folded into the bar vertex color, reverts on cast stop; (b) options panel is BILINGUAL (en/zh), switch on the General page (mutually-exclusive radio, needs /reload; `Locale.lua` + `ns.L` + `ns.ApplyLocale`); (c) ALL cast-bar settings DECOUPLED per-group (height/normal color/important color; width always follows the healthbar) on each group's sub-page; important cast = a clean R->L reverse-fill bar in the important color -- the glow + gold-border emphasis were REMOVED (Jerry wanted just the bar); old global cast settings migrated in. (d) CAST-TARGET BAR (hostile): below the cast bar, right-aligned, a class-colored solid bar + centered target name (`UnitSpellTargetName`/`UnitSpellTargetClass` -- name+class only, NO health under Midnight); per-group cast text size + target bar height/width/text/fallback-color configurable. Implemented: groups 2-5 styled (1 = Blizzard self, 6 = enemy player = Phase 2); threat red/blue + neutral yellow; class/reaction colors; name + level (bar top-right) + health **percent** in-bar (secret-safe `UnitHealthPercent` + integer step curve; absolute amount dropped -- can't abbreviate under secret); target (white + scale) / focus (cyan) highlight; raid marker; tapped dim; critter hide (default off); flat bar texture; cast bar = name + icon + countdown + cast-target + interrupt shield + **important casts** (`C_Spell.IsSpellImportant`) -> orange + gold border + glow + **right-to-left reverse fill** (two-bar trick, works in instances). Options = Blizzard Settings sub-pages (General + per-group). Full how/why in SESSION-LOG.md + GOTCHAS.md.

---

## Release / CurseForge

- **0.9.1 (2026-08-11)** = the 12.1 fix release. **0.9.0 shipped broken and was live on CurseForge for a few hours** (font-less font string handed to `SetApplicationCount` aborted `PLAYER_LOGIN` -> no auras at all + `/dnp` dead; see SESSION-LOG 2026-08-11 and GOTCHAS S3). 0.9.0 also **bypassed this pipeline**: it went out via a one-shot workflow committed and deleted within 40 seconds, through the GitHub API rather than the local tree. Anything published from now on goes through the tag ritual below.
- **LIVE on CurseForge** (project `DodoNameplate`, **ID 1587138**, Author Doodo, category Unit Frames, license All Rights Reserved, distribution = NO 3rd-party). 0.8.0 (Beta) APPROVED 2026-06-25; **0.8.1 (Release, game 12.0.7) auto-published 2026-06-25 via the packager** (tag `v0.8.1` -> GitHub Action -> `DodoNameplate-v0.8.1.zip` on CF + a GitHub release). Default-channel users auto-update to the 0.8.1 Release.
- **Packager WIRED (f3225a1)**: `.github/workflows/release.yml` (BigWigsMods/packager, triggers on tag push) + `## X-Curse-Project-ID: 1587138` in the .toc + `.pkgmeta` ignore list (CLAUDE/DESIGN/GOTCHAS/SESSION-LOG/PENDING-WORK). First release was a MANUAL clean-zip upload (7 lua + toc + Media + README + LICENSE).
- **CF_API_KEY secret = SET (2026-06-25)** on `Baeseata/DodoNameplate` (the existing `github-actions` CF token). The full tag-push -> packager -> CurseForge pipeline is now PROVEN (v0.8.1 shipped first try, run succeeded, upload "Success!").
- **Release ritual from now on**: bump `## Version` in the .toc -> commit -> `git tag vX.Y.Z && git push origin vX.Y.Z` -> the Action auto-packages + uploads to CF. (Claude can cut the tag on request.)
- **JERRY TODO**: CF listing screenshots (gallery) before promoting to 1.0.0.

## Active / next ideas

- **WATCH (possible bug)**: the cast bar's two fills share ONE duration object (`Plate.lua` StartCast: `cb.fillL/fillR:SetTimerDuration(cb.duration, ...)`). If the reverse bar ever doesn't animate, give each fill its own `UnitCastingDuration`/`UnitChannelDuration` call.
- **Elite/rare/boss**: border color done (gold = elite/boss, silver = rare); could add the Blizzard dragon icon if a good atlas is found.
- **Raid-scale validation**: 5-man (Magisters' Terrace) validated; a raid / big-pull M+ pass would be nice.
- Per-group polish as Jerry requests (sizes/colors are all tunable in the options sub-pages).
- **Deferred (Jerry)**: a FRIENDLY-side equivalent of the important-cast health-bar recolor (own separate settings) -- only the hostile sub-page has the recolor toggle/color for now. The generic cast block (width/height/colors/glow) IS on the friendly-NPC sub-page already.
- **WATCH (i18n)**: language switch needs `/reload` because the canvas pages are built once. If Jerry wants live switching later, store every options FontString ref and re-`SetText` on change (verbose) -- noted, not built.

## Aura -- next / deferred

- **SHIPPED 2026-06-24 (a683bec) -- the two extra cats are BUILT, pending in-game test**: a THIRD below-bar-RIGHT buff row routes `HELPFUL` into 可净化增益 (purple, `HELPFUL|RAID_PLAYER_DISPELLABLE`, gated on `ns.playerCanPurge` -- per-class purge table shaman 370 / mage 30449 / priest 528 / druid 2908 / hunter 19801, WARLOCK excluded per Jerry, refreshed on login/spec/SPELLS_CHANGED), 防御性增益 (white, `HELPFUL|BIG_DEFENSIVE`+`EXTERNAL_DEFENSIVE`, enemy PLAYERS only via `f.group==ENEMY_PLAYER`), generic 敌方增益 (green, opt-in). `buffMax=3`; two new panel toggles `chk_auraPurge`/`chk_auraDefensive` (en/zh). luac + a multi-agent secret-safety audit came back CLEAN; NOT yet tested in-game (shipped as Beta knowingly). Priority note: a buff that is both purgeable AND defensive shows purple (purge wins) -- flip if wanted.
- **DECIDE then build -- honour `nameplateShowAllPersonalAuras`**: the main rows hard-require the `nameplateShowPersonal` candidate filter, but Blizzard ORs that flag against the `nameplateShowAllPersonalAuras` CVar (`Blizzard_NamePlateAuras.lua`), so a user who enables the CVar still sees fewer auras from us than from the default UI. The container's candidate filter is strict equality, so "OR" is expressed by OMITTING the key when the CVar is on. Touches BOTH `priority` and `mine` in `ConfigureMain`. Held because it changes the mine-only row decided on 2026-06-26 -- needs Jerry's call first.
- **Per-group aura config**: currently ONE shared `ns.db.auras` for both enemy groups. Split into hostile vs enemy-player if different settings are wanted (e.g. buffs only on players).
- **CC bucket is coarse** (accepted): `HARMFUL|CROWD_CONTROL` includes slows/snares; 12.0 has no stun/root-only token (`C_LossOfControl` is player-only).
- ⚠ **The bullets above predate the v0.9.0 rewrite**: the 12.0 mechanism they describe (manual enumeration, blizzSet gating, per-spellID reasoning) was replaced wholesale by `CustomAuraContainerTemplate`. The user-facing categories survived; the implementation notes did not. Read GOTCHAS S3 for what the code actually does now. (Per the pending-work gates, the "still needs an in-game PvP / raid pass" bullet that used to live here was dropped -- smoke-test scope belongs in CLAUDE.md's Workflow section, not in a queue nobody can ever tick off.)
- **Localize console prints**: `/dnp test` + the "loaded" line are English-only (out of panel scope) -- localize if wanted.
- **Locale default decision**: shipped `locale = "enUS"` (forces English on fresh installs, incl. zhCN clients). Switch to `"auto"` (follow client) if Jerry prefers zhCN clients to auto-get 中文.

## Dropped / deferred (per Jerry -- do not re-suggest)

- **"Dim / shrink the OTHER mobs when one casts an IMPORTANT spell"** (Jerry idea 2026-06-24, SHELVED -- revisit only if Secret Values softens): impossible cross-plate in instances. The `IsSpellImportant` flag is SECRET there, so you cannot read it as a Lua branch, cannot aggregate N plates' secret flags into a global "is anyone important-casting", and there is no sink that dims plate B from plate A's secret (same wall as NPC-id boss mods). "Shrink" is doubly dead -- only alpha + color have boolean sinks, not size/scale. Only feasible shapes: per-plate (every enemy plate sits at a base alpha and pops to full while IT casts important, via `SetAlphaFromBoolean`), or an open-world-only version gated on `not issecretvalue(important)`. See GOTCHAS S3.
- Distance scaling (fixed-size is fine).
- Health absolute AMOUNT text -- impossible to abbreviate under secret (see GOTCHAS S2); only percent is shown.
- Empowered (Evoker) staged casts.

## Phase 2 -- PvP

- **BASE DONE (v0.8.0)**: enemy player (group 6) styled = class-colored bar (guarded `UnitClassBase` -> red fallback when class is secret) + cast bar; added a `groups[6]` block + "Enemy Player" sub-page; default ON; one path covers war mode / BG / arena. Cast path hardened (`HasValue`) so a SECRET cast name in active PvP does not error.
- **Watch / next (probe in-game per context)**: is `UnitClassBase` readable in BG vs arena vs war mode (else the bar is red)? Does the enemy cast bar populate (name / duration) in an active PvP match, or is it more locked than instances? `/dnp test` prints `class:` to check.
- **CUT (walled by SecretInActivePvPMatch -- do not build)**: enemy CD / buff / interrupt / DR tracking, and enemy-healer detection. Display-only `C_SpellDiminish` (DR) + private auras are the only sanctioned leftovers.

## Open questions (confirm with Jerry)

- Healer threat-color treated as DPS (no aggro = red, has aggro = blue).
- Friendly groups (party/raid, other players, friendly NPC) default OFF.
- Overlay defaults: dim tapped ON, hide critters OFF.
- Run a tiny `issecretvalue` probe in raid / M+ to map which calls go secret where (confirmed in-combat + restricted maps so far; cast spellID is secret in instances).
